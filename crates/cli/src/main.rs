//! Headless CLI driving the emu86-core CPU: run and trace 8086 programs.

fn main() {
    // Placeholder binary so the workspace builds end-to-end.
    // Real argument parsing (clap) arrives with the first execution slice.
    std::process::exit(0);
}
